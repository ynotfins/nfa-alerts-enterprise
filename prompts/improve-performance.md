# Improve Performance

I'm experiencing performance issues with [component/page/feature] in the NFA Alerts codebase. [Describe what's slow - page load time, rendering lag, etc]. Please analyze and optimize this code by adding pagination to Convex queries if returning many items, creating proper indexes for frequently queried fields, memoizing expensive calculations with useMemo, wrapping callbacks with useCallback to prevent unnecessary re-renders, implementing virtualization for long lists over 100 items, debouncing search inputs and frequent updates, lazy loading heavy components with React.lazy and Suspense, and optimizing images before upload. Profile the changes to ensure they actually improve performance without breaking functionality.

[Optional: Add current metrics, target performance goals, or specific bottlenecks here]
