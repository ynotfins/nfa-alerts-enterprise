# Fix Bug

I'm experiencing an issue in the NFA Alerts codebase. [Describe what's broken or not working as expected]. The expected behavior is [what should happen] but instead [what actually happens]. Please explore the codebase to identify the root cause - check for missing useQuery dependencies, incorrect firestore query arguments, auth/permission issues, type mismatches, or race conditions. Once you find the issue, implement a fix that follows our patterns and doesn't introduce new bugs. Make sure to test the fix with different user roles and verify real-time updates still work correctly. Also add any necessary guards or validations to prevent similar issues in the future.

[Optional: Add error messages, steps to reproduce, or additional context here]
